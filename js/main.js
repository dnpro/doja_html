(function ($, window, document) {

    // The $ is now locally scoped 
    $(function () {

        // DOM ready!
        //Variables
        $document = $(document),
            $window = $(window),
            $body = $("body");

        // Initial setup
        
/*        $("#ajax-content").find(".text").mCustomScrollbar({
            autoHideScrollbar:true
        });
        */
        // Event delegation

    });

    // Functions


}(window.jQuery, window, document)); // Fully reference jQuery after this point.